/**
 * PeerStocks shared module — wire contract for the PEER_STOCKS response
 * (mirrors server/quotes/service.ts getPeerStocks).
 */
export { type PeerStocksResponse, type PeerStock } from "./schema";
